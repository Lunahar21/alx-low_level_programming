#include "main.h"

/**
 *_strlen_recursion - a function that returns the length of a string
 * @s: string length of a string
 * Return: intenger
 */

int _strlen_recursion(char *s)
{
	if (*s)
	{
		_strlen_recursion(char *s);
		_putchar(s);
}
