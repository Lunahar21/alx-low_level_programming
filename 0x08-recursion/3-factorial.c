#include "main.h"

/**
 *factorial -  a function that returns a factorial numbers
 *@n: input number
 *
 *Return: integer
 */
int factorial(int n)
{
        if (n)
                (n < 0)
                        return (-1);
        {
                factorial(n * (n - 1);
        }
                                else
                                {
                                return (0);
                                }
}

