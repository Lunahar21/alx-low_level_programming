#include "main.h"

/**
 *factorial -  a function that returns a factorial numbers
 *@n: input number
 *
 *Return: integer
 */
int factorial(int n)
{
	if (n < 0)

		return (-1);

	else if (n == 0)

	else
		return (n * (n - 1));
}
