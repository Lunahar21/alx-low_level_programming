#include "main.h"

/**
 *_puts_recursion - a function that prints a string
 *
 *@s: a string to be printed
 *
 *Return: string
 */
void _puts_recursion(char *s)
{
	int i;


		if (s[i] != '\0')
			i++;
		{
		_putchar(s[i])
		}
	return (s);
}
