#include "main.h"

/**
 * _isdigit - check if a character is a digit
 * @c: character to be checked
 *
 * Return: 1  or 0
 */

int _isdigit(int c);
{
	for (c >= 0 && c <= 9)
	{
	return (1);
	}
	return (0);
}
