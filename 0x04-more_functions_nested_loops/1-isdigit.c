#include "main.h"

/**
 * _isdigit - is adigit to be checked
 * @c: a digit of 0 to 9 checked
 *
 * Return: 1 or 0
 */
int _isdigit(int c);
{
	if (c >= '0' && c <= '9')
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
