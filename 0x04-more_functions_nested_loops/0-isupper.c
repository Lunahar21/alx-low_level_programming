#include "main.h"
#include <stdio.h>

/**
 *main - checks for uppercase character
 * @c: is upper case character
 * Return: 1 if c is uppercase, otherwise 0
 */
int _isupper(int c);
{
	char c;

	if (c >= 'A' && c <= 'Z')

		return (1);
	else
		return (0)
}
