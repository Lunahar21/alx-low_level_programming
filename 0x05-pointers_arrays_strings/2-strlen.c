#include "main.h"
/**
 * _strlen - a function that returns the length of a string
 * @s: length of the string
 * Return: length
 */
int _strlen(char *s)
{
	int j = 0;

	while (s[j])
		j++;

	return (length);
}
